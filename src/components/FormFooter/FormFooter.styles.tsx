import { styled } from '../../stitches.config';

export const Container = styled('div', {
  position: 'absolute',
  bottom: '0',
  display: 'flex',
  justifyContent: 'space-between',
  width: '100%',
  height: '4rem',
  alignItems: 'center',
  background: 'white',
});
